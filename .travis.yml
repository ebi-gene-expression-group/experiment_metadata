before_install:
  - if test -e $HOME/miniconda/bin; then
        echo "miniconda already installed.";
    else
      if [[ "$TRAVIS_PYTHON_VERSION" == "2.7" ]]; then
        wget https://repo.continuum.io/miniconda/Miniconda2-latest-Linux-x86_64.sh -O miniconda.sh;
      else
        wget https://repo.continuum.io/miniconda/Miniconda3-latest-Linux-x86_64.sh -O miniconda.sh;
      fi
      
      rm -rf $HOME/miniconda;  
      bash miniconda.sh -b -p $HOME/miniconda;
      export PATH="$HOME/miniconda/bin:$PATH";
      hash -r;
      conda config --set always_yes yes --set changeps1 no;
      conda update -q conda;
  
      conda info -a;

      conda config --add channels defaults;
      conda config --add channels conda-forge;
      conda config --add channels bioconda;
      conda config --add channels ebi-gene-expression-group;

      conda create -q -n test-environment python=$TRAVIS_PYTHON_VERSION perl perl-atlas-modules;
    fi

install:
    - export PATH="$HOME/miniconda/bin:$PATH";
    - source activate test-environment

before_script:
    - export PATH=`pwd`:$PATH

cache:
  directories:
    - $HOME/miniconda

script:
    - bash single_cell_condensed_sdrf.sh -e E-MTAB-6077 -f $(pwd)/test_data/E-MTAB-6077/E-MTAB-6077.idf.txt -o $(pwd) -z $(pwd)/test_data/zooma_exclusions.yml 
    - env ATLAS_SC_EXPERIMENTS=$(pwd)/test_data bash single_cell_condensed_sdrf.sh -e E-MTAB-6077 -o $(pwd) -z $(pwd)/test_data/zooma_exclusions.yml 

before_cache:
    - if ! [[ $TRAVIS_TAG ]]; then rm -rf $HOME/miniconda/conda-bld; fi
    - rm -rf $HOME/miniconda/locks $HOME/miniconda/pkgs $HOME/miniconda/var $HOME/miniconda/conda-meta/history


